{
	"name": "swe_interview",
	"version": "1.0.0",
	"description": "API to compare your pokemons",
	"main": "index.js",
	"type": "module",
	"scripts": {
		"prestart": "npm install",
		"dev": "nodemon index.js",
		"start": "node index.js",
		"unit-test": "NODE_OPTIONS=--experimental-vm-modules NODE_ENV=test jest -t unit",
		"integration-test": "NODE_OPTIONS=--experimental-vm-modules NODE_ENV=test jest -t integration",
		"test": "NODE_OPTIONS=--experimental-vm-modules NODE_ENV=test jest --watchAll"
	},
	"dependencies": {
		"axios": "^0.27.2",
		"dotenv": "^16.0.1",
		"express": "^4.18.1"
	},
	"devDependencies": {
		"axios-mock-adapter": "^1.21.1",
		"jest": "^28.1.3",
		"jest-mock-axios": "^4.6.1",
		"supertest": "^6.2.4"
	}
}
